/**
 * @file normal.frag
 * @brief 法线片段着色器
 * @details 实现了一个基础的片段着色器，直接输出顶点颜色，不进行任何颜色处理
 * 
 * 功能特点：
 * 1. 直接传递顶点颜色
 * 2. 无颜色处理或修改
 * 3. 固定不透明度为1.0
 * 4. 最简单的片段着色器实现
 * 
 * 使用方法：
 * 1. 作为最基础的片段着色器使用
 * 2. 与任何顶点着色器配合
 * 3. 适合需要显示原始颜色的场景
 * 
 * 参数说明：
 * - vertexColor: 从顶点着色器传入的颜色
 * 
 * 自定义修改：
 * 1. 添加颜色处理：在输出前对颜色进行修改
 * 2. 添加透明度控制：增加alpha uniform变量
 * 3. 添加颜色混合：实现与其他颜色的混合
 * 4. 添加简单的光照计算：基于法线计算光照
 */

#version 330 core
// 从顶点着色器接收的颜色
in vec3 vertexColor;
// 输出的片段颜色
out vec4 FragColor;

void main()
{
    // 直接输出顶点颜色，不透明度固定为1.0
    FragColor = vec4(vertexColor, 1.0);
} 